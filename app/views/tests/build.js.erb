$.notify({
  message: "<%= @response_msg %>"
},{
  type: "info",
  delay: 1000
});

$('#build-link-<%= @test.id %>').click(function () {
  return false;
});

setTimeout(function () {
  $('#build-link-<%= @test.id %>').unbind('click');
}, 60000);
