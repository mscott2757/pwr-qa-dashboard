$('#build-link-<%= @test.id %>').click(function () {
  return false;
});

setTimeout(function () {
  $('#build-link-<%= @test.id %>').unbind('click');
}, 60000);

$.notify({
  message: "<%= flash[:info] %>"
},{
  type: "info"
});
