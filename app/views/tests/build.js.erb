<%= render partial: "layouts/flash_message", locals: { type: "info", msg: flash[:info] } %>

$('#build-link-<%= @test.id %>').click(function () {
  return false;
});

setTimeout(function () {
  $('#build-link-<%= @test.id %>').unbind('click');
}, 60000);
