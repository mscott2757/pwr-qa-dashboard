<%= render partial: '/layouts/settings_header', locals: { edit_tests: true } %>

<%= react_component 'Tests', { data: @tests, applications: @applications, environments: @environments, types: @types } %>
