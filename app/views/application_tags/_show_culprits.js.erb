<% culprits.each do |app| %>
  $.notify({
    message: "<%= culprit_format(app, @env_tag) %>",
    url: "<%= application_tag_path(id: app.id, method: "tests") %>"
  },{
    type: 'danger',
    delay: 2000
  });
<% end %>
