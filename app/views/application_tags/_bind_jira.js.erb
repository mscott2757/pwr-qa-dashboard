$("#jira-toggle-<%= app.id %>").click(function() {
  if ($("#sub-<%= app.id %>").css("display") == "none") {
    $("#sub-<%= app.id %>").css("display", "block");
  } else {
    $("#sub-<%= app.id %>").hide();
  }
});
